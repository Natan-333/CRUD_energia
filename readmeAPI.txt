Use o banco de dados local crud com o comando em mysql:ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'fiap23';

por fim use SELECT * FROM crud.usuarios; para ver a tabela usuarios no banco.

na IDE va para o API da pasta Frontend e use yarn start e espere conectr-se com o banco.